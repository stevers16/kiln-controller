# Kiln Controller - Kivy app dependencies
# Pin to known-good versions; bump deliberately.

kivy==2.3.1
requests==2.32.3

# Phase 7 - History graphs. matplotlib embedded via kivy_garden.matplotlib
# (FigureCanvasKivyAgg). The Phase 15 buildozer.spec lists matching
# requirements for python-for-android.
matplotlib==3.10.8
kivy_garden.matplotlib==0.1.1.dev0

# Phase 15 - Android packaging. plyer surfaces the system document
# picker on Android via `kilnapp.platform_helpers.pick_file()`. On
# desktop it's harmless (no plyer.filechooser back-end loads outside
# Android, so the helper falls through to Kivy's FileChooser).
plyer==2.1.0

# KivyMD will be added in Phase 1 when the bottom-nav shell is built.
# kivymd>=2.0.1.dev0
